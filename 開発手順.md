# Datapack CrossRef 実装手順書

## 0. 開発方針

Datapack CrossRefは、TypeScriptで開発する。

VS Code拡張の機能は大きく分けると以下になる。

```text
Datapackを探索
    ↓
ファイルを識別
    ↓
参照を検出
    ↓
逆参照を構築
    ↓
.mcfunctionのヘッダーを生成
    ↓
参照先へのリンクを提供
```

最初からJSONや全リソース種別に対応する必要はない。

まず、

```text
.mcfunction
    ↓
function namespace:foo
    ↓
参照先を検出
```

だけを完成させる。

その後、JSONやクリック可能なリンクを追加する。

---

# Phase 1: VS Code拡張の開発環境を作る

## 実装するべきもの

以下を準備する。

* Node.js
* VS Code
* TypeScript
* VS Code Extension開発環境
* 拡張機能の雛形プロジェクト

最終的に、VS Codeのデバッグ機能から拡張機能を起動できる状態にする。

目標は、

```text
F5
↓
Extension Development Hostが起動
↓
自作拡張が読み込まれる
```

という状態。

## ヒント

VS Code拡張は通常、

```text
src/
└─ extension.ts
```

が最初のエントリーポイントになる。

まずは `activate()` が呼ばれることだけを確認する。

例えば、

```text
拡張機能が有効化された
```

というメッセージをVS Codeに表示できれば、最初の段階は完了。

この段階ではDatapackを扱わない。

---

# Phase 2: コマンドを作る

## 実装するべきもの

VS Codeのコマンドパレットから実行できるコマンドを作る。

例:

```text
Datapack CrossRef: Scan Datapack
```

実行すると、

```text
Datapack CrossRefを実行しました
```

と表示する。

## ヒント

VS Code拡張では、

```text
package.json
```

にコマンドを登録し、

```ts
vscode.commands.registerCommand()
```

で処理を登録する。

処理の流れは、

```text
package.json
    ↓
コマンドIDを登録
    ↓
extension.ts
    ↓
同じコマンドIDをregisterCommandで登録
    ↓
コマンドパレットから実行
```

となる。

ここで、`package.json` の設定とTypeScriptのコードがどのようにつながるかを理解する。

---

# Phase 3: 現在のワークスペースを取得する

## 実装するべきもの

現在VS Codeで開かれているフォルダを取得する。

例えば、

```text
MyDatapack/
```

をVS Codeで開いている場合、そのルートフォルダを取得する。

目標:

```text
workspace folder
↓
ファイルシステム上のDatapackルート
```

を扱えるようにする。

## ヒント

VS Code APIの、

```ts
vscode.workspace.workspaceFolders
```

を調べる。

重要なのは、ワークスペースが常に存在するとは限らないこと。

以下の場合を考える。

```text
VS Codeでフォルダを開いている
→ workspaceFoldersが存在する

単一ファイルだけを開いている
→ workspaceFoldersが存在しない可能性がある
```

そのため、`undefined` の処理を必ず考える。

---

# Phase 4: Datapackらしい構造か確認する

## 実装するべきもの

現在のワークスペースがDatapackとして扱えるか確認する。

最低限、例えば、

```text
pack.mcmeta
```

や、

```text
data/
```

の存在を確認する。

目標:

```text
Datapack
→ スキャンする

Datapackではない
→ エラーメッセージを表示する
```

という分岐を作る。

## ヒント

VS Code APIでは、

```ts
vscode.workspace.fs.stat()
```

を使用すると、ファイルやディレクトリの存在を確認できる。

確認対象のURIは、

```ts
vscode.Uri.joinPath()
```

で組み立てる。

この段階で、OSごとのパス区切り文字を自分で処理しないこと。

Windowsの、

```text
\
```

とLinux/macOSの、

```text
/
```

を意識する必要があるため、VS CodeのURI APIを使う。

---

# Phase 5: Datapack内のファイルを再帰的に探索する

## 実装するべきもの

Datapackの `data/` 以下を再帰的に探索する。

例えば、

```text
data/
├─ namespace_a/
│  └─ function/
│     ├─ tick.mcfunction
│     └─ test/
│        └─ foo.mcfunction
└─ namespace_b/
   └─ function/
      └─ bar.mcfunction
```

を探索して、すべての `.mcfunction` を取得する。

最終的には、

```text
[
  tick.mcfunction,
  foo.mcfunction,
  bar.mcfunction
]
```

のような結果を得る。

## ヒント

まず自分で再帰処理を書いてみる。

基本的な考え方は、

```text
ディレクトリを読む
    ↓
各項目を確認

ファイル
→ 一覧に追加

ディレクトリ
→ そのディレクトリを再帰的に読む
```

VS Code APIでは、

```ts
vscode.workspace.fs.readDirectory()
```

を調べる。

返されるデータが、

```text
名前 + FileType
```

という形であることを確認する。

この処理は今後、

* `.mcfunction`
* `.json`

などを探索する基礎になる。

---

# Phase 6: FunctionファイルからResource IDを生成する

## 実装するべきもの

`.mcfunction` の実際のファイルパスから、

```text
namespace:path
```

形式のFunction IDを生成する。

例:

```text
data/example/function/tick.mcfunction
```

↓

```text
example:tick
```

また、

```text
data/example/function/test/foo.mcfunction
```

↓

```text
example:test/foo
```

とする。

## ヒント

パスを以下の要素に分解して考える。

```text
data
namespace
function
relative/path
file.mcfunction
```

必要なのは、

```text
namespace
+
function/以下の相対パス
```

である。

変換後に、

```text
function/
```

と、

```text
.mcfunction
```

を取り除く。

この処理は専用の関数として作る。

例えば概念的には、

```text
function URI → Function ID
```

という責務だけを持たせる。

他の処理と混ぜない。

---

# Phase 7: Function IDとファイルの対応表を作る

## 実装するべきもの

すべての `.mcfunction` を探索した後、

```text
Function ID
↓
実際のファイル
```

という対応表を作る。

例:

```text
example:tick
→ data/example/function/tick.mcfunction

example:test/foo
→ data/example/function/test/foo.mcfunction
```

## ヒント

TypeScriptの、

```ts
Map
```

を使うことを検討する。

概念的には、

```text
Map<string, vscode.Uri>
```

のような構造になる。

これは今後、

```text
function example:tick
```

を発見したとき、

```text
example:tick
↓
実際のファイル
```

を高速に解決するために使う。

この対応表は「IDからファイルを引くための索引」と考える。

---

# Phase 8: `.mcfunction` の内容を読む

## 実装するべきもの

取得した `.mcfunction` を読み込む。

例えば、

```mcfunction
say Hello
function example:foo
function example:test/bar
```

という内容を文字列として取得できるようにする。

## ヒント

VS Code APIの、

```ts
vscode.workspace.fs.readFile()
```

を調べる。

注意点は、返される値がそのまま文字列ではないこと。

必要に応じて、

```ts
TextDecoder
```

を使って文字列へ変換する。

この段階では、まだ参照を解析しなくてよい。

まず、

```text
ファイルを取得できる
↓
内容を文字列として読める
```

ことだけを確認する。

---

# Phase 9: `function` コマンドを検出する

## 実装するべきもの

`.mcfunction` の各行からFunction参照を検出する。

例:

```mcfunction
function example:tick
```

↓

```text
example:tick
```

を取得する。

複数ある場合、

```mcfunction
function example:a
function example:b
function example:c
```

から、

```text
[
  example:a,
  example:b,
  example:c
]
```

を取得する。

## ヒント

最初は複雑な正規表現を書かない。

まず、

```text
行ごとに分割
↓
空白を処理
↓
functionで始まるか確認
↓
次の値を取得
```

という単純な方法で試す。

その後、以下を考える。

```mcfunction
# function example:tick
```

コメントなので無視する。

また、実際のMinecraftコマンドでは、

```text
execute ... run function example:tick
```

のような間接的な出現もある。

最初のPhaseでは、

```text
行頭のfunctionコマンド
```

だけに限定してよい。

対応範囲を広げるのは後で行う。

---

# Phase 10: 逆参照を作る

## 実装するべきもの

ここがDatapack CrossRefの中心機能。

通常の参照は、

```text
foo.mcfunction
↓
function example:tick
↓
tickを参照
```

となる。

これを逆にして、

```text
example:tick
↓
example:foo が参照している
```

という情報を作る。

例:

```text
example:tick
├─ example:foo
├─ example:bar
└─ example:baz
```

## ヒント

データ構造を先に紙やテキストで考える。

例えば、

```text
対象Function ID
↓
参照元一覧
```

という構造。

概念的には、

```text
Map<
  string,
  Reference[]
>
```

になる。

スキャン処理では、

```text
各.mcfunctionを読む
    ↓
参照先を検出
    ↓
参照先IDを取得
    ↓
そのIDの逆参照一覧に
「現在のファイル」を追加
```

という順番。

ここまで完成すると、

```text
example:tickはどこから参照されているか
```

を取得できる。

---

# Phase 11: 参照元を種類ごとに管理する

## 実装するべきもの

現段階ではFunctionしかないが、将来JSONなどを追加するため、

```text
参照元の種類
+
参照元のID
+
参照元のファイルURI
```

をまとめたデータ構造にする。

例:

```text
function
example:foo
data/example/function/foo.mcfunction
```

## ヒント

この段階で、

```text
stringだけの配列
```

から、

```text
オブジェクトの配列
```

へ変更する。

概念:

```ts
type Reference = {
    type: string;
    id: string;
    uri: vscode.Uri;
};
```

ここでは最初から複雑にしすぎない。

後で、

```text
advancement
enchantment
```

を追加できる形にすることが目的。

---

# Phase 12: `.mcfunction` のヘッダーを生成する

## 実装するべきもの

逆参照情報から、以下のようなヘッダーを文字列として生成する。

```mcfunction
# example:tick
# function:
#     example:foo
#     example:bar
```

参照元がない場合は、

```mcfunction
# example:tick
```

だけにする。

## ヒント

ファイルへの書き込み処理と、ヘッダー生成処理は分離する。

まず、

```text
Function ID
+
逆参照情報
↓
ヘッダー文字列
```

だけを行う関数を作る。

例えば入力が、

```text
example:tick

function:
- example:foo
- example:bar
```

なら、出力が、

```text
# example:tick
# function:
#     example:foo
#     example:bar
```

になる。

この関数はファイルを読んだり書いたりしない。

文字列を作ることだけを担当させる。

---

# Phase 13: ヘッダーを実際のファイルへ書き込む

## 実装するべきもの

生成したヘッダーを `.mcfunction` の先頭に追加する。

例:

変更前:

```mcfunction
say Hello
function example:foo
```

変更後:

```mcfunction
# example:tick
# function:
#     example:bar

say Hello
function example:foo
```

## ヒント

まずはテスト用のDatapackで実装する。

流れは、

```text
ファイルを読む
↓
既存内容を取得
↓
ヘッダーを生成
↓
ヘッダー + 改行 + 元の内容
↓
ファイルへ書き込む
```

となる。

VS Code APIでは、

```ts
vscode.workspace.fs.writeFile()
```

を調べる。

文字列は、

```ts
TextEncoder
```

でバイト列に変換する。

---

# Phase 14: ヘッダーの重複を防ぐ

## 実装するべきもの

拡張機能を2回実行しても、

```mcfunction
# example:tick

# example:tick
```

のようにならないようにする。

## ヒント

この問題は必ず先に設計する。

方法は2つある。

### 方法A: 専用マーカーを使う

```mcfunction
# === Datapack CrossRef Start ===
# example:tick
# function:
#     example:foo
# === Datapack CrossRef End ===

say Hello
```

更新時にStartからEndまでを置き換える。

実装は非常に簡単。

ただし、最終的な見た目に専用コメントが残る。

### 方法B: マーカーを使わない

先頭のコメントを解析する。

例えば、

```text
# namespace:path
# function:
#     namespace:path
```

という構造を認識して置き換える。

見た目はよいが、ユーザーが書いたコメントとの区別が難しくなる。

最初の実装では方法Aを推奨する。

機能が完成してから、方法Bを検討する。

---

# Phase 15: `DocumentLinkProvider` を実装する

## 実装するべきもの

生成されたIDをクリック可能にする。

例えば、

```mcfunction
# function:
#     example:foo
```

の、

```text
example:foo
```

をクリックすると、

```text
data/example/function/foo.mcfunction
```

を開く。

## ヒント

VS Codeの、

```ts
DocumentLinkProvider
```

を調べる。

基本的な考え方は、

```text
Document
↓
リンクにする文字列を探す
↓
その文字列の位置をRangeとして取得
↓
リンク先のURIを作る
↓
DocumentLinkを返す
```

となる。

ここでは、

```text
表示文字列
```

と、

```text
実際のURI
```

が別物であることを理解する。

---

# Phase 16: Functionリンクを実装する

## 実装するべきもの

まず、

```mcfunction
# function:
#     example:foo
```

を検出し、

```text
data/example/function/foo.mcfunction
```

へのリンクを作る。

## ヒント

すでにPhase 7で、

```text
Function ID
↓
Functionファイル
```

の対応表を作っている。

その情報を再利用できるか考える。

ただし、`DocumentLinkProvider` が呼ばれるタイミングと、スキャン時のデータが常に一致するとは限らない。

まずは必要に応じて、

```text
example:foo
↓
namespace = example
path = foo
↓
data/example/function/foo.mcfunction
```

と直接URIを作る方法でもよい。

---

# Phase 17: 同じIDが別種類に存在する問題に対応する

## 実装するべきもの

以下が同時に存在する場合を考える。

```text
data/example/advancement/test.json
data/example/enchantment/test.json
data/example/function/test.mcfunction
```

ヘッダー上ではすべて、

```text
example:test
```

という文字列になる。

しかし、リンク先は異なる。

```mcfunction
# advancement:
#     example:test

# enchantment:
#     example:test

# function:
#     example:test
```

それぞれ正しいファイルを開く。

## ヒント

文字列だけでリンク先を判断しない。

重要なのは、

```text
どのセクションに書かれているか
```

である。

処理は、

```text
現在の行を確認
↓
上方向、または先頭から現在位置まで解析
↓
現在のセクションを取得
↓
advancement / enchantment / functionを決定
↓
ID + 種類から実際のファイルパスを作る
```

という形にする。

例えば、

```text
type = advancement
id = example:test
```

なら、

```text
data/example/advancement/test.json
```

になる。

---

# Phase 18: JSONファイルを探索する

## 実装するべきもの

`.mcfunction` だけではなく、

```text
advancement/
enchantment/
```

などのJSONファイルを取得する。

まずはJSONファイル一覧を作る。

例:

```text
data/example/advancement/a.json
data/example/advancement/b.json
data/example/enchantment/c.json
```

## ヒント

Phase 5の再帰探索処理を再利用する。

その後、ファイルパスから、

```text
namespace
resource type
relative path
```

を取り出す。

例えば、

```text
data/example/advancement/test/foo.json
```

なら、

```text
namespace = example
type = advancement
id = example:test/foo
```

となる。

---

# Phase 19: JSONをパースする

## 実装するべきもの

JSONファイルを読み込み、JavaScript/TypeScriptのオブジェクトとして扱えるようにする。

## ヒント

流れは、

```text
ファイルを読む
↓
TextDecoderで文字列にする
↓
JSON.parse()
↓
オブジェクトとして扱う
```

となる。

注意点として、Datapackの作成途中などではJSONが壊れている可能性がある。

そのため、

```text
JSON.parseに失敗
↓
拡張機能全体を停止させない
↓
エラー情報を記録
↓
次のファイルを処理
```

という設計にする。

---

# Phase 20: JSONを再帰的に探索する

## 実装するべきもの

JSONの深さに依存せず、内部を探索できる処理を作る。

例えば、

```json
{
  "rewards": {
    "function": "example:tick"
  }
}
```

だけでなく、

```json
{
  "something": {
    "deep": {
      "inside": {
        "function": "example:tick"
      }
    }
  }
}
```

のような場合も探索する。

## ヒント

JSONには主に以下がある。

```text
string
number
boolean
null
array
object
```

再帰処理は、

```text
値を確認

object
→ すべてのプロパティを再帰

array
→ すべての要素を再帰

その他
→ 終了
```

という基本構造になる。

ここで重要なのは、JSONの「階層」を処理することと、「Minecraftの参照として意味がある値」を判定することを分けること。

---

# Phase 21: JSON内のFunction参照を判定する

## 実装するべきもの

JSON内から、

```text
Functionへの参照
```

だけを検出する。

例:

```json
{
  "rewards": {
    "function": "example:tick"
  }
}
```

↓

```text
example:tick
```

## ヒント

すべての文字列をFunction IDとして扱わない。

例えば、

```json
{
  "description": "example:tick"
}
```

があった場合、それはFunction参照ではない可能性がある。

まずは、

```text
キーがfunction
かつ
値が文字列
```

という条件から実装する。

その後、Minecraftの各データ形式を調査して、

```text
どのキーが
どのリソースIDを参照するのか
```

というルールを追加していく。

この部分は将来的に、

```text
JSON参照ルール
```

として別ファイルに分離することを検討する。

---

# Phase 22: JSONからの逆参照を追加する

## 実装するべきもの

例えば、

```text
advancement/test.json
```

が、

```text
example:tick
```

を参照している場合、

`example:tick` の逆参照一覧へ、

```text
type = advancement
id = example:test
```

を追加する。

結果:

```text
example:tick
├─ advancement
│  └─ example:test
└─ function
   └─ example:foo
```

## ヒント

Phase 10で作った逆参照インデックスを拡張する。

Function専用の構造を作り直すのではなく、

```text
Reference
├─ type
├─ id
└─ uri
```

という共通構造を使う。

ここまで分離できていれば、

```text
参照元がFunctionか
JSONか
```

に関係なく同じインデックスへ登録できる。

---

# Phase 23: ヘッダーを種類ごとに生成する

## 実装するべきもの

逆参照を種類ごとにまとめる。

例:

```text
advancement
- example:adv_a
- example:adv_b

enchantment
- example:ench_a

function
- example:foo
```

これを、

```mcfunction
# example:tick
# advancement:
#     example:adv_a
#     example:adv_b
# enchantment:
#     example:ench_a
# function:
#     example:foo
```

に変換する。

## ヒント

以下の順番で処理を分ける。

```text
逆参照一覧
↓
typeごとに分類
↓
typeごとにIDを並べる
↓
ヘッダー文字列を生成
```

一つの関数ですべて処理しない。

また、参照が存在しない種類は、

```text
# advancement:
```

のような空セクションを作らない。

---

# Phase 24: リンク先が存在する場合だけリンクを作る

## 実装するべきもの

参照先ファイルが存在しない場合、クリック可能なリンクを作らない。

例:

```mcfunction
# function:
#     example:not_exists
```

が存在しても、

```text
data/example/function/not_exists.mcfunction
```

が存在しないならリンクを提供しない。

## ヒント

`DocumentLinkProvider` 内で、

```text
IDからURIを作る
↓
ファイルが存在するか確認
↓
存在する場合だけDocumentLinkを追加
```

という処理を行う。

ただし、DocumentLinkProviderは頻繁に呼ばれる可能性がある。

毎回Datapack全体をスキャンしない。

まずは単純な実装で動かし、必要になった場合にキャッシュを検討する。

---

# Phase 25: ファイル変更への対応

## 実装するべきもの

Datapack内のファイルが変更されたとき、情報を更新できるようにする。

例えば、

```text
foo.mcfunctionを変更
↓
function example:tickを追加
↓
再スキャン
↓
tick.mcfunctionの逆参照情報を更新
```

## ヒント

最初は、

```text
手動でScan Datapackコマンドを実行
```

だけでよい。

その後、

```text
ファイル保存時
```

や、

```text
ファイル作成・削除時
```

に更新する機能を追加する。

VS Code APIにはファイル変更を監視する仕組みがある。

ただし、自動更新を最初から実装すると、

```text
ファイル変更
↓
拡張機能がヘッダーを書き換える
↓
その変更を再び検出
↓
再スキャン
```

のような循環が発生する可能性がある。

そのため、まず手動実行で完成させる。

---

# 推奨する最初の完成目標

まず、以下だけを完成させる。

```text
1. Datapackを開く

2. コマンドを実行する

3. data/以下の.mcfunctionをすべて取得する

4. 各ファイルのFunction IDを生成する

5. function namespace:pathを検出する

6. 逆参照を作る

7. 各.mcfunctionのヘッダーを書き込む
```

この段階では、

```text
JSON
DocumentLinkProvider
自動更新
```

は実装しない。

最初の完成形は、

```text
Datapack CrossRefの基本機能
```

だけにする。

---

# 推奨する実装順

最終的には以下の順序で進める。

```text
Phase 1
VS Code拡張の雛形
    ↓
Phase 2
コマンド作成
    ↓
Phase 3
ワークスペース取得
    ↓
Phase 4
Datapack構造確認
    ↓
Phase 5
ファイル再帰探索
    ↓
Phase 6
Function ID生成
    ↓
Phase 7
ID → ファイル対応表
    ↓
Phase 8
.mcfunction読み込み
    ↓
Phase 9
function参照検出
    ↓
Phase 10
逆参照インデックス
    ↓
Phase 11〜14
ヘッダー生成・更新
    ↓
ここで最初の完成版
    ↓
Phase 15〜17
DocumentLinkProvider
    ↓
Phase 18〜23
JSON解析
    ↓
Phase 24
存在しないリンクの除外
    ↓
Phase 25
ファイル変更への対応
```

---

# 開発中に意識すること

## 一度に複数の機能を実装しない

例えば、

```text
ファイル探索ができていない
+
Function ID変換が未完成
+
逆参照も未完成
+
ヘッダー書き込みも追加
```

という状態で一度に実装しない。

必ず、

```text
1機能を実装
↓
動作確認
↓
次の機能
```

と進める。

---

## テスト用Datapackを作る

実際の大きなDatapackではなく、小さなテスト環境を用意する。

```text
test-datapack/
├─ pack.mcmeta
└─ data/
   └─ example/
      └─ function/
         ├─ tick.mcfunction
         ├─ foo.mcfunction
         └─ test/
            └─ bar.mcfunction
```

例えば、

`foo.mcfunction`:

```mcfunction
function example:tick
```

`test/bar.mcfunction`:

```mcfunction
function example:tick
```

とする。

期待結果は、

`tick.mcfunction`:

```mcfunction
# example:tick
# function:
#     example:foo
#     example:test/bar
```

になる。

このように、各Phaseごとに「入力」と「期待する結果」を用意する。

---

# 最初に調べるTypeScript / VS Code API

実装前に以下を順番に調べる。

```text
TypeScript
- async / await
- Promise
- Map
- Array
- string
- union type
- interface
- type
- try / catch
- JSON.parse
- 正規表現の基本

VS Code Extension API
- ExtensionContext
- commands.registerCommand
- workspace.workspaceFolders
- workspace.fs.readDirectory
- workspace.fs.readFile
- workspace.fs.writeFile
- workspace.fs.stat
- Uri
- Uri.joinPath
- DocumentLinkProvider
- DocumentLink
- Range
```

この順序で進めれば、TypeScriptとVS Code Extension APIを学びながら、Datapack CrossRefの機能を段階的に完成させられる。
