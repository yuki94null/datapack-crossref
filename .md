#

| 起点 | 仕組み | 備考 |
| ------------ | --------------------------------- | ------------------------------ |
| コマンド | `/function <id>` | 直接実行 |
| コマンド | `execute if/unless function <id>` | function の存在・成功を評価しつつ実行 |
| コマンド | `/schedule function <id>` | 遅延実行 |
| Function Tag | `#minecraft:load` | データパック読み込み時 |
| Function Tag | `#minecraft:tick` | 毎tick |
| Function Tag | その他の `#namespace:path` | `/function #namespace:path` など |
| Advancement | `rewards.function` | Advancement 達成時 |
| Enchantment | `run_function` | エンチャント効果から実行 |
| Text Components | click_event等 | テキストのある場所から実行するので参照が曖昧になりやすい |

commandの文脈であれば、
'function 'を探索すれば良い

jsonの'run_function'系の文脈であれば、
'"function"'を探索すれば良い

アイテム等(loot_table, item_modifier, recipe)のtext componentsのclick eventを考慮する場合は、
'function 'を探索する

ただし`.mcfunction`では`\`による改行を考慮する必要がある

```a.mcfunction
f/
u/
n/
c/
t/
i/
o/
n/

abc
```

などである
また、`\`の後ろには文字が来うるので、改行までは無視する必要がある。
