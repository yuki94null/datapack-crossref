# datapack-crossref 修正手順

## 1. 背景：保存が循環する原因

自動保存(`files.autoSave`)がオンの状態で `onDidSaveTextDocument` から `makeCrossRef()` を呼んでいるため、以下の流れでイベントが再発火してしまう。

```.txt
保存
  → makeCrossRef() 実行
    → updateHeaders() 内の applyEdit でファイルを書き換え(dirty化するだけで保存はしない)
  → headerCreating が false に戻る
  → 自動保存が dirty なファイルを保存
  → onDidSaveTextDocument が再度発火
    → headerCreating は既に false なのでガードをすり抜ける
  → makeCrossRef() が再度実行される
```

`applyEdit` は保存扱いにならないため、`headerCreating` フラグでは自動保存経由の再発火を防げない。これが循環の直接原因。

さらに、`allFiles` / `UriToMcPath` / `McPathToUri` / `ref` がスキャンのたびにクリアされずに追記され続ける(`vscode.Uri` は参照比較のため、再スキャンで生成される新しい Uri は古いエントリと同一視されない)ため、循環が起きた場合に状態が際限なく膨張する要因にもなっている。

---

## 2. 修正方針

対応方針は「保存をトリガーにした自動実行をやめ、コマンド実行のみで動作させる」。これにより循環の入口自体をなくす。

### 2-1. `headerCreating` フラグを削除

保存トリガー自体をなくすため、二重発火防止用だったこのフラグは不要になる。

```ts
// 削除
let headerCreating = false;
```

### 2-2. `onDidSaveTextDocument` リスナーを削除

```ts
// このブロックを丸ごと削除
vscode.workspace.onDidSaveTextDocument(async (document) => {
    if (headerCreating) return;
    if (
        !document.fileName.endsWith(".json") &&
        !document.fileName.endsWith(".mcfunction")
    ) {
        return;
    }
    headerCreating = true;
    try {
        await makeCrossRef();
    } finally {
        headerCreating = false;
    }
});
```

以後、`scanDatapack` / `makeCrossRef` / `removeCrossRef` はコマンドパレットから手動実行する運用にする。

### 2-3. 再スキャン時に索引用 Map/Set をクリアする

`reference.ts` の `ref` を `extension.ts` から参照できるよう import に追加。

```ts
import { addReference, ref, REFERENCE_TYPE } from "./reference";
```

`createPathDictionary` および `buildReference` の実行前(各コマンド実行の起点)で、以下をクリアしてから再構築する。

```ts
allFiles.clear();
UriToMcPath.clear();
McPathToUri.clear();
ref.clear();
```

これにより、コマンドを複数回実行しても古い Uri エントリが残り続けることがなくなる。

---

## 3. 構造的な改善案(優先度順)

循環の修正とあわせて、保守性向上のために以下の改善を検討する。

### 3-1. 索引情報の集約(優先度：高)

`allFiles` / `UriToMcPath` / `McPathToUri`(`extension.ts`)と `ref`(`reference.ts`)は、本来「データパックの索引」という一つの概念であるにもかかわらず2ファイルに分散し、生の `Map`/`Set` として直接 export されている。誰でも直接書き換えられる状態は、クリア漏れのようなバグの温床になる。

**改善案：** 索引専用モジュール(例：`index.ts`)を作り、状態をクラス等にまとめて内部に閉じ込める。

```ts
class DatapackIndex {
    private allFiles = new Set<vscode.Uri>();
    private uriToPath = new Map<vscode.Uri, string[]>();
    private pathToUri = new Map<string, vscode.Uri>();
    private refs = new Map<vscode.Uri, References>();

    clear() { /* ... */ }
    register(uri: vscode.Uri, path: string[]) { /* ... */ }
    addReference(source: vscode.Uri, type: string, target: vscode.Uri) { /* ... */ }
    getByPath(path: string) { /* ... */ }
}
export const datapackIndex = new DatapackIndex();
```

2章の「再スキャン時のクリア」もこの中で一元管理すれば、クリア漏れのリスクが構造的になくなる。

### 3-2. コマンド関数の共通処理の切り出し(優先度：高)

`scanDatapack` / `makeCrossRef` / `removeCrossRef` は冒頭のワークスペース検証〜ファイル取得部分がほぼ重複している。

```ts
const wf = vscode.workspace.workspaceFolders;
if (!wf) { vscode.window.showErrorMessage('No WorkSpace Here'); return; }
const mcmetaUri = vscode.Uri.joinPath(wf[0].uri, 'pack.mcmeta');
if (!(await fileCheck(mcmetaUri) === vscode.FileType.File)) { /* ... */ return; }
const dataUri = vscode.Uri.joinPath(wf[0].uri, "data");
const dataUris = await getFiles(dataUri);
createPathDictionary(dataUris);
```

**改善案：** 共通関数に切り出す。

```ts
async function getDatapackFiles(): Promise<vscode.Uri[] | undefined> {
  const wf = vscode.workspace.workspaceFolders;
  if (!wf) {
    vscode.window.showErrorMessage('No WorkSpace Here');
    return undefined;
  }
  const mcmetaUri = vscode.Uri.joinPath(wf[0].uri, 'pack.mcmeta');
  if (!(await fileCheck(mcmetaUri) === vscode.FileType.File)) {
    vscode.window.showErrorMessage('This is not Datapack Workspace');
    return undefined;
  }
  const dataUri = vscode.Uri.joinPath(wf[0].uri, "data");
  return await getFiles(dataUri);
}
```

3つのコマンドはこの関数の戻り値を使うだけにする。datapack 判定ロジックの修正が1箇所で済むようになる。

### 3-3. `findFuncInFile` の分割(優先度：中)

現状、参照タイプ(`REFERENCE_TYPE`)ごとの JSON 構造解析が7分岐の if/else として1関数に集約されており、タイプが増えるたびに肥大化する構造になっている。

**改善案：** タイプごとの抽出処理を関数化し、ディスパッチテーブルで引く。

```ts
const extractors: Record<string, (json: unknown) => string[]> = {
  "tags/function": extractFromTagList,
  "advancement": extractFromAdvancement,
  "enchantment": extractFromEnchantment,
  "dialog": extractFromDialog,
  "loot_table": extractFromCommandJson,
  "recipe": extractFromCommandJson,
  "item_modifier": extractFromCommandJson,
};
```

新しい参照タイプの追加時、既存の if/else に手を入れず関数を1つ追加するだけで済むようになる。

### 3-4. `createLinks` の責務分割(優先度：中)

以下の2つの処理が1関数に同居している。

- ヘッダー内の type 区切り位置(`typeIndexes`)の計算
- 各 Uri について正規表現マッチしリンクを生成する処理

**改善案：** 前半を `getTypeIndexes(text: string): [string, number][]` として切り出し、`createLinks` はリンク生成のみに専念させる。

### 3-5. `REFERENCE_TYPE` の型安全化(優先度：低〜中)

`REFERENCE_TYPE` は `string[]`、`addReference` の `type` 引数も生の `string` であり、タイプミスは実行時例外でしか検出できない。

**改善案：**

```ts
export const REFERENCE_TYPE = [
  "tags/function", "function", "advancement", "dialog",
  "enchantment", "loot_table", "recipe", "item_modifier",
] as const;
export type ReferenceType = typeof REFERENCE_TYPE[number];
```

`addReference(uri, type: ReferenceType, uri)` とすることで、タイプ名のスペルミスをコンパイル時に検出できる。

### 3-6. `createHeader` の文字列組み立て(優先度：低)

`addTexts.join("\n" + HEADER_PREFIX)` で各行に prefix を付与する実装は動作するが、意図が読み取りにくい。以下のような小さいヘルパーを挟むと可読性が上がる。

```ts
const line = (text: string) => HEADER_PREFIX + text;
```

---

## 4. 実施順序の目安

1. 2章の循環修正(`headerCreating` 削除、`onDidSaveTextDocument` 削除、Map/Set クリア処理の追加)
2. 3-1 索引情報の集約 — 循環修正の「クリア処理」ともっとも関係が深いため、続けて着手すると効率が良い
3. 3-2 コマンド関数の共通処理の切り出し
4. 3-3, 3-4 の関数分割
5. 3-5, 3-6 の型安全化・可読性向上(余力があれば)
